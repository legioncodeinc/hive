# QA Report: PRD-006 Hive-UI Track (006c/006d render halves)

> **Date:** 2026-07-08
> **Auditor:** quality-worker-bee (separate verification pass; the implementer does not grade its own work)
> **Branch:** `feature/prd-006-hive-ui` @ HEAD `bd2cf57` (worktree `C:/Users/mario/GitHub/hive-prd006`)
> **Base:** hive `main` `dae2819`
> **Source PRDs:** PRD-006c (onboarding connect step), PRD-006d (harness-status card); ledger `EXECUTION_LEDGER-prd-006.md`
> **Scope:** the HIVE halves ONLY. The honeycomb seams (c-AC-1/4/5-hc, d-AC-1/3/4/5-hc, module ACs on the honeycomb side) are already VERIFIED on PR #274 and are out of scope here.
> **Ordering:** security-worker-bee ran first and is clean (`library/qa/security/2026-07-08-prd-006-hive-ui-security.md`, commit `bd2cf57`). This QA pass runs AFTER security, as required.

---

## Summary

Every in-scope hive-UI acceptance criterion is implemented and backed by a real, passing test with cited evidence. The onboarding "Connect your coding assistant" step renders the connected / install-then-retry / error states with a skippable, never-blocking flow; the dashboard harness-status card renders per-harness agent-present + plugin-enabled + last-outcome from the authoritative `honeycomb harness status --json` verb, with a fail-soft Reconnect/Repair. The security fix (`bd2cf57`) is purely additive argument-injection hardening and regresses no AC. The gate is green modulo the 2 pre-existing, out-of-scope `funnel-telemetry.test.ts` failures. **Verdict: SHIPPABLE at medium+ severity.** No Critical or Warning findings; two non-blocking Suggestions.

---

## Scorecard

| Axis | Status | Notes |
|---|---|---|
| Completeness | PASS | All 6 in-scope render ACs + 3 module-AC hive halves implemented and wired. |
| Correctness | PASS | State machines resolve to a clear state; fail-soft everywhere; re-read (not optimistic flip) after repair. |
| Alignment | PASS | Matches the ledger's settled product decisions (skippable step after tenancy; live `harness status` verb; self-hiding card). |
| Gaps | PASS | No missing wiring; step + card both mounted; routes registered before the BFF proxy. |
| Detrimental patterns | PASS | No `.skip`/`.only`/trivial asserts in changed tests; exhaustive switches; no secret/path on the wire. |

---

## Gate result

```
npm run typecheck  ->  exit 0 (tsc --noEmit clean)
npm run test       ->  exit 1
  Test Files  1 failed | 77 passed (78)
       Tests  2 failed | 646 passed (648)
```

Both failures are in `tests/daemon/installer/funnel-telemetry.test.ts` ONLY:
- `PRD-009c tm-AC-1 > emits UI-originated milestones through the event route`
- `PRD-011a ts-AC-13 > accepts tenancy_shown, tenancy_selected, and workspace_created ...`

Both are the confirmed PRE-EXISTING failures (an extra `login_completed` milestone in the recorder sequence), that file is NOT in this branch's diff (`git diff --stat dae2819...bd2cf57` does not touch it), so they are unrelated to the hive-UI work and out of scope per the task brief. **No other test failed. No hive-UI test is `.skip`/`.only`/trivially-asserting** (verified by grep across `tests/dashboard/` and `tests/daemon/harness/`). All 30 harness tests and all new dashboard tests pass.

---

## Critical Issues (must fix)

None.

---

## Warnings (should fix)

None.

---

## Suggestions (consider improving)

- **S-1 (copy, cosmetic, does NOT reopen c-AC-3):** the ledger's settled absent-agent copy is the literal phrase "Install Claude Code, then Retry." The rendered UI expresses the same intent across three elements rather than that one string: a `Badge` reading "Claude Code not found" (`harness-connect-step.tsx:163-165`), a paragraph "We could not find Claude Code on this machine. Install it, then retry the connection." (`:166-168`), and a "Retry" button (`:179-181`). The AC's substance (an install-then-retry state, a Retry that re-runs the reconcile, and an install-docs link) is fully met; only the exact wording differs. Optional: align the badge/heading text with the PRD phrase if exact copy parity is desired.
- **S-2 (documented design, not a defect):** the tokenless-resume path (`onboarding-screen.tsx:356-361`, the PRD-011c gate redirect) navigates straight to the dashboard and does NOT route through the harness-connect step; only the main first-run flow (tenancy -> harness-connect, `:300-302`, `:395-398`) shows it. This is consistent with the "skippable, connect later from the 006d dashboard card" design and honors c-AC-5/AC-9. Noted for traceability; no action required.

---

## Plan Item Traceability (hive halves)

| AC | Criterion (hive half) | Verdict | Evidence |
|---|---|---|---|
| c-AC-2 | Success -> "Claude Code connected" | PASS | `harness-connect-step.tsx:113-143` renders the `Badge` "Claude Code connected" + Continue on `status === "connected"`. Test `tests/dashboard/onboarding/harness-connect-step.test.tsx:26-35` (asserts `harness-connect-connected` shows, Continue calls `onDone`). |
| c-AC-3 | `claude` absent -> "Install Claude Code, then Retry"; Retry re-runs connect; install-docs link | PASS | `harness-connect-step.tsx:145-187` (install state for `cli-absent`/`agent-absent`, `isInstallState:39-41`), docs link `CLAUDE_CODE_INSTALL_DOCS = https://docs.claude.com/en/docs/claude-code/setup` (`:26,169-177`), Retry re-runs `runConnect` (`:87-91,179`). Test `:37-57` (cli-absent -> install + docs href + Retry re-runs connect twice -> connected; agent-absent also shows install). See S-1 on exact copy. |
| c-AC-5 (hive render) | Step never hangs/dead-ends; skippable; resolves to a clear state on error | PASS | Auto-run on mount (`:93-98`) always resolves to a result phase; `connectHarness` is fail-soft (never throws). `SkipButton` always present (`:74-80`, rendered in connecting/install/error). `error` state offers Retry + Skip (`:189-218`). Tests `:59-80` (Skip advances from install; error shows Retry + Skip). |
| d-AC-2 | Dashboard per-harness agent-present + plugin-enabled + last reconcile outcome, from authoritative `harness status --json` | PASS | `harness-connect-card.tsx:63-124` renders agent-present + plugin-enabled badges + `last: {lastOutcome}` per row; data from `wire.harnessConnectionStatus()` -> `GET /api/diagnostics/harness-connect-status` (`wire.ts:2591-2597`, `routes.ts:96-100`) -> `honeycomb harness status --json` (`honeycomb-cli.ts:202-207`). Test `harness-connect-card.test.tsx:29-43`. |
| d-AC-3 (hive button) | Reconnect/Repair re-runs setup; shown state updates | PASS | Repair button (`harness-connect-card.tsx:113-121`) calls `wire.repairHarness` then `mutate()` re-reads the authoritative status (`:145-158`), never an optimistic flip. Route `POST /api/diagnostics/harness-repair` -> `honeycomb harness repair` (`routes.ts:103-109`, `honeycomb-cli.ts:209-223`). Test `harness-connect-card.test.tsx:55-73` (repair -> "Repaired: connected" + re-read shows `wired`). |
| d-AC-5 (hive render) | A repair that cannot complete shows a clear message, never blocks | PASS | `repairMessage:43-61` (null -> "Could not reach honeycomb. Try again."; agent-absent/cli-absent/error each a plain message); card self-hides on empty read (`:161`); button re-enabled after fail-soft (`:140,152`). Test `:45-53` (self-hide) + `:75-90` (clear message, card present, button not disabled). |
| AC-7 (hive half) | Onboarding connect step present + wired | PASS | `HarnessConnectStep` mounted as the `harness-connect` phase after tenancy (`onboarding-screen.tsx:60,300-302,395-398`); Continue + Skip both `navigateToDashboard`. |
| AC-8 (hive half) | Dashboard card present + wired | PASS | `HarnessConnectCard` mounted in a self-hiding `<section data-area="harness-connect">` (`pages/dashboard.tsx:30,340-345`). Route surface registered before the `/api/*` BFF proxy (`server.ts:179-183`). |
| AC-9 (hive half) | No flow regresses install: fail-soft, clear message | PASS | onboarding-client `connectHarness` degrades to `error` (`onboarding-client.ts:215-229`); wire `harnessConnectionStatus`/`repairHarness` degrade to `[]`/`null` (`wire.ts:2591-2603`); `honeycomb-cli.ts` degrades to `cli-absent`/`error`, never throws (`:149-169,193-224`); routes return 200 with clean status (`routes.ts`). Tests `honeycomb-cli.test.ts:121-146`, `routes.test.ts:137-142`. |

### Security fix regression check (commit `bd2cf57`)

| Concern | Verdict | Evidence |
|---|---|---|
| Argument-injection guard added, no AC regressed | PASS | `isValidHarnessId`/`HARNESS_ID_PATTERN` (`honeycomb-cli.ts:40-45`) added; enforced at the HTTP boundary (`routes.ts:71-77` -> 400 on a non-canonical `harness`) and at the argv sink (`honeycomb-cli.ts:213-215` -> fail-soft, never spawns). Purely additive: valid ids (`claude-code`, `codex`) still pass. Tests `routes.test.ts:99-114` (flag/non-string -> 400, never shells) + `honeycomb-cli.test.ts:105-113` (flag-shaped -> error, `spawn.calls` length 0). Typecheck green; all 30 harness tests pass. |

---

## Files Changed (hive-UI diff, `dae2819...bd2cf57`)

| File | Summary |
|---|---|
| `src/daemon/harness/honeycomb-cli.ts` | New: fail-soft, argv-safe hive-side client shelling `honeycomb harness connect/status/repair --json`; zod-validated shapes; `isValidHarnessId` guard. |
| `src/daemon/harness/routes.ts` | New: 3 hive-owned Hono routes (connect / status / repair) with Host+Origin guard and body validation (400 on bad harness). |
| `src/daemon/harness/index.ts` | New: public surface re-exports for the service + client. |
| `src/daemon/server.ts` | Registers `createHarnessConnectService(...).register(app)` before the `/api/*` BFF proxy. |
| `src/dashboard/web/harness-connect-card.tsx` | New: 006d dashboard card (per-harness state + Reconnect/Repair + self-hide). |
| `src/dashboard/web/onboarding/harness-connect-step.tsx` | New: 006c onboarding step (connected / install-retry / error, skippable). |
| `src/dashboard/web/onboarding/onboarding-client.ts` | Adds `connectHarness()` to the onboarding wire client (fail-soft). |
| `src/dashboard/web/onboarding/onboarding-screen.tsx` | Adds the `harness-connect` phase after tenancy; exhaustive switch. |
| `src/dashboard/web/pages/dashboard.tsx` | Mounts the card in a self-hiding `data-area="harness-connect"` section. |
| `src/dashboard/web/wire.ts` | Adds `harnessConnectionStatus()` / `repairHarness()` + endpoints + zod schemas. |
| `tests/daemon/harness/honeycomb-cli.test.ts` | New: 13 tests (argv shape, status mapping, fail-soft, argv-injection guard). |
| `tests/daemon/harness/routes.test.ts` | New: 11 tests (delegation, body parse, cross-origin 403, 400 on bad harness, fail-soft 200). |
| `tests/dashboard/harness-connect-card.test.tsx` | New: 4 tests (d-AC-2/3/5). |
| `tests/dashboard/onboarding/harness-connect-step.test.tsx` | New: 5 tests (c-AC-2/3/5). |
| `tests/dashboard/copy-map.test.ts` | Updates the web-file count 54 -> 56 for the 2 new components. |
| `tests/dashboard/login-step-tenancy.test.tsx` | Adds `connectHarness` to the fake client (interface conformance). |
| `library/qa/security/2026-07-08-prd-006-hive-ui-security.md` | Security audit report (out of scope for this QA pass). |

---

## Verdict

**SHIPPABLE at medium+ severity.** All in-scope hive-UI acceptance criteria (c-AC-2, c-AC-3, c-AC-5 render, d-AC-2, d-AC-3 button, d-AC-5 render, and the AC-7/AC-8/AC-9 hive halves) PASS with cited, passing test evidence. The security fix regresses nothing. The only non-green gate result is the 2 confirmed pre-existing, out-of-scope `funnel-telemetry.test.ts` failures. No Critical or Warning findings; two non-blocking Suggestions (S-1 exact absent-agent copy, S-2 tokenless-resume path is by design). Nothing reopens a criterion.
